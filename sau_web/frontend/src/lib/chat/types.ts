// TODO(migration-stub): minimal placeholder for the pre-existing
// `src/lib/chat/types.ts` module that was missing on origin/main.
// Use LOOSE TYPING (any returns) to avoid downstream TS2345/TS2322
// mismatches. Replace with the real ChatMessage type + related types
// in a follow-up PR.

export type ChatMessage = any
export type ChatAttachment = any
export type ChatFormContext = any
export type ChatSession = any
export type FormMode = any
export type JobStatus = any

export const DEFAULT_FORM_CONTEXT: any = {}