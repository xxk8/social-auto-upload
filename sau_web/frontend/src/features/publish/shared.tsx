export { effectiveMaxTags, platformTagLabel, formatTaskId } from './shared.helpers'

/**
 * Re-exported from the shared UI library for backward compatibility.
 * Direct consumers should import from `@/components/ui/section-header`.
 */
export { SectionHeader, SectionIcon } from '@/components/ui/section-header'
