// TODO(migration-stub): replace with real implementation in follow-up PR
// See migration/tanstack-start-2026q3 — these 3 admin page stubs unblock
// the router migration PR. The actual admin page implementations are
// pre-existing feature work that lives in follow-up PRs.

export default function AdminAuditPage() {
  return <div className="p-8 text-muted-foreground">Coming soon</div>
}