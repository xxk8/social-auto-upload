# 运行前提（百家号 / baijiahao-upload）

需要满足 `python sau` 主线的运行前提，详见 [`docs/install.md`](../../docs/install.md)。要点：

- Python 3.10+ 与项目虚拟环境
- PostgreSQL 14+（推荐，Web Shell 默认数据库）
- Chrome / Chromium 浏览器（通过 `patchright install chromium` 准备）

百家号本身没有特殊的额外依赖，仅依赖通用 patchright + cookie 加载流程。
