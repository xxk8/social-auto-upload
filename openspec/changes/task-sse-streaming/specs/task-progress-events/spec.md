## ADDED Requirements

### Requirement: Task Progress Events (openspec delta-format stub)
The `Task Progress Events` capability is added by openspec change `task-sse-streaming`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Task Progress Events` workflow is invoked per `openspec/changes/task-sse-streaming/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/task-sse-streaming/proposal.md` and `openspec/changes/task-sse-streaming/design.md`
