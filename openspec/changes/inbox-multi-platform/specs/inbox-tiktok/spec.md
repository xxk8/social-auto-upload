## ADDED Requirements

### Requirement: Inbox TikTok (openspec delta-format stub)
The `Inbox TikTok` capability is added by openspec change `inbox-multi-platform`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Inbox TikTok` workflow is invoked per `openspec/changes/inbox-multi-platform/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/inbox-multi-platform/proposal.md` and `openspec/changes/inbox-multi-platform/design.md`
