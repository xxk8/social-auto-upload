## ADDED Requirements

### Requirement: Baijiahao Schedule Time (openspec delta-format stub)
The `Baijiahao Schedule Time` capability is added by openspec change `fix-baijiahao-schedule-time`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Baijiahao Schedule Time` workflow is invoked per `openspec/changes/fix-baijiahao-schedule-time/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/fix-baijiahao-schedule-time/proposal.md` and `openspec/changes/fix-baijiahao-schedule-time/design.md`
