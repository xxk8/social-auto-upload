# AC #4 Probe — `2026-07-02` (UTC)

> Companion to `docs/dev/baijahigao-schedule-manual-test-2026q3.md`. This file is the **evidence record** — the operator's actual captured dropdown contents + the decision that closes the openspec ticket (or opens a downstream one).

**Operator**: `<FILL_IN — staging account operator name>`
**Browser**: `<FILL_IN — Chrome version + patchright version>`
**Account type**: `<FILL_IN — staging / sandbox / production-never>` (NEVER production)
**Web Runner URL observed**: `<FILL_IN — observed URL when dropdowns opened; or "raw headed Chrome, no run.py">`
**Probe run timestamp (UTC)**: `<FILL_IN — iso8601>`

---

## Day dropdown capture (sanity check)

> Goal: confirm the day dropdown renders as expected (`{month}月{day}日`, with `0{d}日` zero-pad for `day < 10`). Mismatches here would invalidate the day-side `>> text=` selector too.

**Selector used**: `div.rc-virtual-list div.cheetah-select-item`
**Captured `all_inner_texts()`**: `<FILL_IN — paste the literal list, e.g. ['7月15日', '7月16日', '7月17日', '7月18日', '7月19日', '7月20日', '7月21日']>`
**Length**: `<FILL_IN — count of items>`
**First item**: `<FILL_IN — first element, e.g. '7月15日'>`
**Last item**: `<FILL_IN — last element, e.g. '7月21日'>`

**Verdict**: `<FILL_IN — match `7月X日` format? Y / N>`

---

## Hour dropdown capture (PRIMARY evidence for AC #4)

> Goal: confirm whether the platform renders hour options as `{N}点` (matching the design-D1 fix assumption) or some other format (`{N}时`, `HH:MM`, with `上午/下午` prefix, etc.).

**Selector used**: `div.rc-virtual-list div.cheetah-select-item-option`
**Captured `all_inner_texts()`**: `<FILL_IN — paste the literal list>`

```
# Paste the raw output verbatim in this code block for byte-for-byte fidelity.
# Example expected output if format matches design-D1:
# ['0点', '1点', '2点', '3点', '4点', '5点', '6点', '7点', '8点', '9点', '10点', '11点', '12点', '13点', '14点', '15点', '16点', '17点', '18点', '19点', '20点', '21点', '22点', '23点']
```

**Length**: `<FILL_IN — count of items, e.g. 24>`
**First item**: `<FILL_IN>`
**Last item**: `<FILL_IN>`
**Format detection** (one of):
- `[ ]` `{N}点` — e.g. `8点`, `14点`, `22点` (matches design-D1 → CLOSE-OK)
- `[ ]` `{N}时` — e.g. `8时`, `14时` (format-mismatch → OPEN-DOWNSTREAM-TICKET-SIMPLE)
- `[ ]` `HH:MM` — e.g. `08:00`, `14:00`, `22:00` (format-mismatch + minute granularity likely → OPEN-DOWNSTREAM-TICKET-MEDIUM)
- `[ ]` `上午/下午 HH:MM` — e.g. `上午 8:00`, `下午 2:00` (format-mismatch + AM/PM parsing → OPEN-DOWNSTREAM-TICKET-COMPLEX)
- `[ ]` Other (describe): `<FILL_IN — paste observed format>`

---

## Minute granularity (secondary deliverable)

> Goal: record whether the hour dropdown exposes minute-level slots (e.g. `8:00`, `8:30`, `9:00`) — this affects whether to also wire a minute dropdown in a downstream ticket.

**Inspection method**: `<FILL_IN — describe>` (e.g. "scanned the {N}点 list; every option is a single integer hour; no minute subdivision").
**Result**: `<FILL_IN — one of: "none" / "exposed: 30-min slots" / "exposed: 15-min slots" / "other: <describe>">`

**If minute is exposed, paste verbatim**:

```
# <FILL_IN>
```

---

## Decision block (must be filled before this probe can be referenced)

> This block is the **terminal state** for AC #4. The openspec `tasks.md §5.2 / §5.3` cannot be marked complete until this block has a definitive answer.

**Decision**: `<FILL_IN — one of: CLOSE-OK / OPEN-DOWNSTREAM-TICKET-SIMPLE / OPEN-DOWNSTREAM-TICKET-MEDIUM / OPEN-DOWNSTREAM-TICKET-COMPLEX>`
**Rationale**: `<FILL_IN — 1-3 sentences explaining the decision>`
**Downstream ticket path** (if applicable): `<FILL_IN — path to openspec/changes/<name>/ if a downstream ticket is opened; else "N/A">`
**Downstream ticket PR scope** (if applicable): `<FILL_IN — describe the planned change: which file(s), what line(s), what new test(s)>`

---

## Probe close-out checklist (operator self-verification)

Before saving this file:

- [ ] All `<FILL_IN — ...>` slots are populated (no placeholders remaining).
- [ ] Day dropdown paste is **byte-for-byte verbatim** (no autoformatting).
- [ ] Hour dropdown paste is in a fenced code block (preserves whitespace + unicode).
- [ ] Format detection checkbox is checked.
- [ ] Decision block has a definitive answer (not "TBD" / "uncertain").
- [ ] Cross-references in `tasks.md§5.2/5.3` updated.
- [ ] File committed with message `chore(probe): AC#4 baijiahao-schedule probe — 2026-07-02 — <decision>`.

---

## Cross-references

- `docs/dev/baijahigao-schedule-manual-test-2026q3.md` — the procedure this evidence was generated from.
- `uploader/baijiahao_uploader/main.py::BaiJiaHaoVideo.set_schedule_time` — the runtime that the probe's verdict gates.
- `tests/test_baijiahao_set_schedule_time.py` — the regression tests that survive the probe's outcome (substring-safe `{N}点` assumption) or get updated (format-mismatch → additional hour-value cases).
- `openspec/changes/fix-baijiahao-schedule-time/proposal.md` — AC #4 source-of-truth.
- `openspec/changes/fix-baijiahao-schedule-time/design.md` — D1 fix rationale this probe validates.
- `openspec/changes/fix-baijiahao-schedule-time/design.md#d2` — RuntimeError contract (the D2 diagnostic renders `available` list to logs, which is independent of the probe's format detection).
