## ADDED Requirements

### Requirement: Public Inbox Ops Runbook (openspec delta-format stub)
The `Public Inbox Ops Runbook` capability is added by openspec change `public-inbox-monetization`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Public Inbox Ops Runbook` workflow is invoked per `openspec/changes/public-inbox-monetization/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/public-inbox-monetization/proposal.md` and `openspec/changes/public-inbox-monetization/design.md`
