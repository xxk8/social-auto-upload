## ADDED Requirements

### Requirement: Design Token Discipline (openspec delta-format stub)
The `Design Token Discipline` capability is added by openspec change `web-shell-polish-2026-q3`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Design Token Discipline` workflow is invoked per `openspec/changes/web-shell-polish-2026-q3/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/web-shell-polish-2026-q3/proposal.md` and `openspec/changes/web-shell-polish-2026-q3/design.md`
