## ADDED Requirements

### Requirement: Account Health Notification (openspec delta-format stub)
The `Account Health Notification` capability is added by openspec change `account-health-monitoring`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Account Health Notification` workflow is invoked per `openspec/changes/account-health-monitoring/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/account-health-monitoring/proposal.md` and `openspec/changes/account-health-monitoring/design.md`
