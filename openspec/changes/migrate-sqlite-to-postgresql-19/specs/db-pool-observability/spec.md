## ADDED Requirements

### Requirement: DB Pool Observability (openspec delta-format stub)
The `DB Pool Observability` capability is added by openspec change `migrate-sqlite-to-postgresql-19`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `DB Pool Observability` workflow is invoked per `openspec/changes/migrate-sqlite-to-postgresql-19/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/migrate-sqlite-to-postgresql-19/proposal.md` and `openspec/changes/migrate-sqlite-to-postgresql-19/design.md`
