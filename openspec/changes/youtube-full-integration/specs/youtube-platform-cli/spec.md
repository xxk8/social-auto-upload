## ADDED Requirements

### Requirement: YouTube Platform CLI (openspec delta-format stub)
The `YouTube Platform CLI` capability is added by openspec change `youtube-full-integration`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `YouTube Platform CLI` workflow is invoked per `openspec/changes/youtube-full-integration/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/youtube-full-integration/proposal.md` and `openspec/changes/youtube-full-integration/design.md`
