## ADDED Requirements

### Requirement: Crawler Core (openspec delta-format stub)
The `Crawler Core` capability is added by openspec change `media-crawler-integration`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Crawler Core` workflow is invoked per `openspec/changes/media-crawler-integration/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/media-crawler-integration/proposal.md` and `openspec/changes/media-crawler-integration/design.md`
