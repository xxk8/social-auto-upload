## ADDED Requirements

### Requirement: Uploader Migration Tail (openspec delta-format stub)
The `Uploader Migration Tail` capability is added by openspec change `phase5-uploader-migration-tail`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Uploader Migration Tail` workflow is invoked per `openspec/changes/phase5-uploader-migration-tail/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/phase5-uploader-migration-tail/proposal.md` and `openspec/changes/phase5-uploader-migration-tail/design.md`
