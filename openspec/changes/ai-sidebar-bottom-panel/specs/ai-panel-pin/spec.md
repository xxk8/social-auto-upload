## ADDED Requirements

### Requirement: AI Panel Pin (openspec delta-format stub)
The `AI Panel Pin` capability is added by openspec change `ai-sidebar-bottom-panel`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `AI Panel Pin` workflow is invoked per `openspec/changes/ai-sidebar-bottom-panel/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/ai-sidebar-bottom-panel/proposal.md` and `openspec/changes/ai-sidebar-bottom-panel/design.md`
