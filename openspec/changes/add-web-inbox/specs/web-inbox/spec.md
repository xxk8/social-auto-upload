## ADDED Requirements

### Requirement: Web Inbox (openspec delta-format stub)
The `Web Inbox` capability is added by openspec change `add-web-inbox`. This file is a delta-format stub; the full behavior contract is in the change's `proposal.md` and `design.md` (yet to be backfilled into proper Requirement / Scenario entries). The system MUST satisfy this contract per the change's proposal.md and design.md.

#### Scenario: Standard execution path (stub)
- **WHEN** the `Web Inbox` workflow is invoked per `openspec/changes/add-web-inbox/design.md`
- **THEN** the system MUST satisfy the behavioral contract documented in `openspec/changes/add-web-inbox/proposal.md` and `openspec/changes/add-web-inbox/design.md`
